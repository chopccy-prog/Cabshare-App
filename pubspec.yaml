name: cabshare_app
description: Cabshare mobile app
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  dio: ^5.5.0
  flutter_riverpod: ^2.5.1


flutter:
  uses-material-design: true
  # Re-enable assets later after the app boots.
  # assets:
  #   - assets/
  assets:
    - assets/
