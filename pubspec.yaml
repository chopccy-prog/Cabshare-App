name: cabshare_app
description: Cabshare mobile app
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  supabase_flutter: ^2.6.0
  intl: ^0.19.0
  shared_preferences: ^2.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # If/when you add images, create an assets/ folder and uncomment:
  # assets:
  #   - assets/
